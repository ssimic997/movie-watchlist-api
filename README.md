# Movie Watchlist API

--- 

## Overview 

A small REST API that lets authenticated users manage a personal movie watchlist. When a
user addsa small REST API that lets authenticated users manage a personal movie watchlist. When a
user adds a movie, your API should fetch additional details from a public external movie API and
store the relevant data locally. a movie, your API should fetch additional details from a public external movie API and
store the relevant data locally.
